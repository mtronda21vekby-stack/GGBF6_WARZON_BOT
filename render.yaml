services:
  - type: web
    name: GGBF6_WARZON_BOT
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.webhook:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
