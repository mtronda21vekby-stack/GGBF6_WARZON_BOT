# v19 acceptance criteria

The release is accepted when the same mission lifecycle is observable through Telegram and the Mini App, all writes are server-authoritative and idempotent, the full regression suite is green, and the production smoke gate verifies release 19.0.0 plus the Mission Control API and assets.
