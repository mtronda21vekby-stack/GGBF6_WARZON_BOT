# app/worlds/bo7/presets.py
# -*- coding: utf-8 -*-
from __future__ import annotations

from typing import Dict, Any


def _p(prof: Dict[str, Any], key: str, default: str) -> str:
    v = (prof or {}).get(key)
    return str(v).strip() if v else default


def bo7_role_setup_text(profile: Dict[str, Any]) -> str:
    role = _p(profile, "role", "Flex")
    diff = _p(profile, "difficulty", "Normal")
    plat = _p(profile, "platform", "PC")
    inp = _p(profile, "input", "Controller")

    return (
        "🎭 BO7 — Роль (RU)\n\n"
        f"Роль: {role} | Режим: {diff}\n"
        f"Платформа: {plat} | Input: {inp}\n\n"
        "BO7 обычно наказывает за глупые репики быстрее, чем Warzone.\n"
        "Правило элиты:\n"
        "• 1 контакт → 1 килл/урон → СМЕНА позиции.\n\n"
        "Если хочешь максимум — напиши режим (паб/ранкед) и стиль (агро/умно).\n"
    )


def bo7_aim_sens_text(profile: Dict[str, Any]) -> str:
    inp = _p(profile, "input", "Controller")
    diff = _p(profile, "difficulty", "Normal")

    if inp == "KBM":
        return (
            "🎯 BO7 — Aim/Sens (KBM) (RU)\n\n"
            "База:\n"
            "• DPI 800 (или 1600)\n"
            "• Сенса: чтобы трекинг был как по рельсам\n"
            "• ADS: стабильно\n\n"
            f"Режим {diff}:\n"
            "• Normal: точность > скорость\n"
            "• Pro: быстрый микрокоррект\n"
            "• Demon: пик-шоты + агро-стрейф\n"
        )

    return (
        "🎯 BO7 — Aim/Sens (Controller) (RU)\n\n"
        "База:\n"
        "• Deadzone минимум без дрифта\n"
        "• Aim response выбери и держи\n\n"
        f"Режим {diff}:\n"
        "• Normal: спокойная рука\n"
        "• Pro: быстрее доводка\n"
        "• Demon: контроль отдачи на ходу\n"
    )


def bo7_controller_tuning_text(profile: Dict[str, Any]) -> str:
    return (
        "🎮 BO7 — Controller Tuning (RU)\n\n"
        "1) Deadzones:\n"
        "• Left: без дрифта\n"
        "• Right: без дрожи\n\n"
        "2) Кривая:\n"
        "• Если трясёт — плавнее\n"
        "• Если не успеваешь — ближе к линейной\n\n"
        "3) Тест:\n"
        "• 10 минут — и только потом правки.\n"
    )


def bo7_kbm_tuning_text(profile: Dict[str, Any]) -> str:
    return (
        "⌨️ BO7 — KBM Tuning (RU)\n\n"
        "• Убери accel\n"
        "• Оставь одну сенсу на неделю\n"
        "• Пик из угла: инфа → килл → смена\n"
    )


def bo7_movement_positioning_text(profile: Dict[str, Any]) -> str:
    diff = _p(profile, "difficulty", "Normal")
    return (
        "🧠 BO7 — Мувмент/Позиционка (RU)\n\n"
        f"Режим: {diff}\n\n"
        "СЕЙЧАС:\n"
        "• Не стой на одном месте после килла\n"
        "• Играй от выхода, а не от «красивого угла»\n\n"
        "ДАЛЬШЕ:\n"
        "• После каждого файта задай: «где я буду через 3 секунды?»\n"
    )


def bo7_audio_visual_text(profile: Dict[str, Any]) -> str:
    return (
        "🎧 BO7 — Аудио/Видео (RU)\n\n"
        "Аудио:\n"
        "• Сделай шаги читаемыми\n"
        "• Убери лишний шум\n\n"
        "Видео:\n"
        "• Читаемость врага > красота\n"
        "• Меньше мусора = меньше смертей\n"
    )
