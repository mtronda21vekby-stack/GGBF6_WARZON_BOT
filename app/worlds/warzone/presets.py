# app/worlds/warzone/presets.py
# -*- coding: utf-8 -*-
from __future__ import annotations

from typing import Dict, Any


def _p(prof: Dict[str, Any], key: str, default: str) -> str:
    v = (prof or {}).get(key)
    return str(v).strip() if v else default


def wz_role_text(profile: Dict[str, Any]) -> str:
    role = _p(profile, "role", "Flex")
    platform = _p(profile, "platform", "PC")
    input_ = _p(profile, "input", "Controller")
    diff = _p(profile, "difficulty", "Normal")

    return (
        "🎭 Warzone Role Setup\n\n"
        f"Роль: {role}\n"
        f"🖥 {platform} | 🎮 {input_} | 😈 {diff}\n\n"
        "Коротко по роли:\n"
        "• Slayer: добивание/трейд, но не соло-раш.\n"
        "• Entry: первый вход, инфа, не умирать бесплатно.\n"
        "• IGL: коллы, тайминг ротаций, дисциплина.\n"
        "• Support: утилити, ресы/смоки, прикрытие.\n"
        "• Flex: закрываешь дыру в тиме.\n\n"
        "Если хочешь — напиши: “состав тима + кто что делает” и я соберу роли идеально."
    )


def wz_aim_sens_text(profile: Dict[str, Any]) -> str:
    input_ = _p(profile, "input", "Controller")
    diff = _p(profile, "difficulty", "Normal")

    if input_ == "KBM":
        return (
            "🎯 Warzone Aim/Sens (KBM)\n\n"
            "База:\n"
            "• DPI: 800 (или 1600)\n"
            "• Сенса: стабильный трекинг > флики\n"
            "• ADS multiplier: 1.0–1.2 и НЕ прыгать каждый день\n\n"
            f"Режим: {diff}\n"
            "• Normal: чистый трек + контроль спрея\n"
            "• Pro: скорость входа в цель + микро\n"
            "• Demon: пик-шоты + агрессивные свопы углов\n"
        )

    return (
        "🎯 Warzone Aim/Sens (Controller)\n\n"
        "База:\n"
        "• Sens: средняя/высокая, но без дрожи\n"
        "• Deadzone: минимальная без дрифта\n"
        "• Aim response: выбери одну (linear/динамика) и держи неделю\n\n"
        f"Режим: {diff}\n"
        "• Normal: стабильность > скорость\n"
        "• Pro: быстрее вход в трекинг\n"
        "• Demon: агрессия + контроль отдачи на ходу\n"
    )


def wz_controller_tuning_text(profile: Dict[str, Any]) -> str:
    platform = _p(profile, "platform", "PC")
    return (
        "🎮 Warzone Controller Tuning\n\n"
        f"Платформа: {platform}\n"
        "1) Deadzone:\n"
        "• Left: минимальная без дрифта\n"
        "• Right: минимальная без дрожи\n\n"
        "2) Response curve:\n"
        "• если трясёт — чуть плавнее\n"
        "• если не успеваешь — ближе к linear\n\n"
        "3) ADS:\n"
        "• не меняй каждый день — адаптация важнее\n"
    )


def wz_kbm_tuning_text(profile: Dict[str, Any]) -> str:
    return (
        "⌨️ Warzone KBM Tuning\n\n"
        "1) Сенса:\n"
        "• одна база на неделю\n"
        "• ADS без сюрпризов\n\n"
        "2) Мувмент:\n"
        "• пик: 1 инфа → 1 килл → смена позиции\n"
        "• стрейф-контроль во время трекинга\n"
    )


def wz_movement_positioning_text(profile: Dict[str, Any]) -> str:
    role = _p(profile, "role", "Flex")
    return (
        "🧠 Warzone Movement/Positioning\n\n"
        f"Роль: {role}\n"
        "СЕЙЧАС:\n"
        "• не репикай одинаково\n"
        "• после 1 килла — смени угол (особенно если тим рядом)\n"
        "• держи cover-циклы: угол → инфа → сместился\n\n"
        "ДАЛЬШЕ:\n"
        "• 10 минут: пик из укрытия + отмена пика\n"
        "• 10 минут: трекинг по страйфу (без паники)\n"
    )


def wz_audio_visual_text(profile: Dict[str, Any]) -> str:
    return (
        "🎧 Warzone Audio/Visual\n\n"
        "База:\n"
        "• убери лишний шум (музыка/эффекты)\n"
        "• приоритет: шаги / перезарядка / zip / двери\n\n"
        "Визуал:\n"
        "• стабильный FPS важнее “красоты”\n"
        "• если мажешь — часто виноват motion blur / лишние эффекты\n"
    )
