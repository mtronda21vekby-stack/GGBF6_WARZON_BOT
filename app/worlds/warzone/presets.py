# app/worlds/warzone/presets.py
# -*- coding: utf-8 -*-
from __future__ import annotations

from typing import Dict, Any


def _p(prof: Dict[str, Any], key: str, default: str) -> str:
    v = (prof or {}).get(key)
    return str(v).strip() if v else default


def wz_role_setup_text(profile: Dict[str, Any]) -> str:
    role = _p(profile, "role", "Flex")
    diff = _p(profile, "difficulty", "Normal")
    plat = _p(profile, "platform", "PC")
    inp = _p(profile, "input", "Controller")

    return (
        "🎭 Warzone — Роль (RU)\n\n"
        f"Роль: {role} | Режим: {diff}\n"
        f"Платформа: {plat} | Input: {inp}\n\n"
        "Смысл роли (коротко, но по-взрослому):\n"
        "• Slayer — ты забираешь первые 2 килла и ломашь темп врагу.\n"
        "• Entry — ты открываешь файт, но не умираешь бесплатно.\n"
        "• IGL — ты решаешь где жить: ротации/тайминг/темп.\n"
        "• Support — ты делаешь, чтобы тиммейты были бессмертные (почти).\n"
        "• Flex — ты затыкаешь дыры: где надо — там ты.\n\n"
        "Микро-правило (чтобы не быть пластиковым):\n"
        "«1 инфа → 1 решение → 1 действие». Не стоим, не ждём судьбу.\n"
    )


def wz_aim_sens_text(profile: Dict[str, Any]) -> str:
    inp = _p(profile, "input", "Controller")
    diff = _p(profile, "difficulty", "Normal")

    if inp == "KBM":
        return (
            "🎯 Warzone — Aim/Sens (KBM) (RU)\n\n"
            "База (стабильность важнее магии):\n"
            "• DPI: 800 (или 1600 — если рука лёгкая)\n"
            "• Сенса в игре: под контролируемый трекинг\n"
            "• ADS multiplier: не меняй чаще раза в неделю\n"
            "• Mouse accel: OFF\n\n"
            "Калибровка (2 минуты):\n"
            "1) 10 ботов: трекинг без рывков\n"
            "2) 10 ботов: флик → стоп → контроль\n"
            "3) Если перелетаешь — минус сенса, если не доводишь — плюс.\n\n"
            f"Режим {diff}:\n"
            "• Normal: попадать чаще, чем пытаться красиво\n"
            "• Pro: быстрее вход в цель + меньше лишних микродвижений\n"
            "• Demon: агрессия, пик-тайминг, трекинг на муве\n"
        )

    return (
        "🎯 Warzone — Aim/Sens (Controller) (RU)\n\n"
        "База:\n"
        "• Sens: средняя/высокая, но без дрожи\n"
        "• Deadzone: минимальная без дрифта\n"
        "• Aim Response: выбери (Linear/Dynamic) и не прыгай каждый день\n"
        "• Aim Assist: включён (да, мы не герои-мученики)\n\n"
        "Калибровка (2 минуты):\n"
        "1) Трекинг цели вбок 30 сек\n"
        "2) Смена цели 10 раз: не дёргай правый стик\n"
        "3) Если дрожит — чуть выше deadzone/плавнее кривая\n\n"
        f"Режим {diff}:\n"
        "• Normal: тихий прицел + меньше паники\n"
        "• Pro: быстрее коррект, меньше овершутов\n"
        "• Demon: агрессия на муве + контроль отдачи при стрейфе\n"
    )


def wz_controller_tuning_text(profile: Dict[str, Any]) -> str:
    diff = _p(profile, "difficulty", "Normal")
    return (
        "🎮 Warzone — Controller Tuning (RU)\n\n"
        f"Режим: {diff}\n\n"
        "1) Deadzones:\n"
        "• Left: минимальная без дрифта\n"
        "• Right: минимальная без дрожи\n\n"
        "2) Кривая стика:\n"
        "• Если трясёт — более плавная\n"
        "• Если не успеваешь — ближе к linear\n\n"
        "3) ADS мультипликаторы:\n"
        "• Низкий zoom: стабильность\n"
        "• Высокий zoom: контроль, не скорость\n\n"
        "4) Главное правило:\n"
        "Меняешь 1 параметр → играешь 20 минут → только потом решаешь.\n"
    )


def wz_kbm_tuning_text(profile: Dict[str, Any]) -> str:
    diff = _p(profile, "difficulty", "Normal")
    return (
        "⌨️ Warzone — KBM Tuning (RU)\n\n"
        f"Режим: {diff}\n\n"
        "1) Сенса:\n"
        "• Одна база на неделю\n"
        "• ADS без сюрпризов\n\n"
        "2) Видимость:\n"
        "• Убери лишние эффекты, которые мешают видеть пик\n"
        "• Контраст/резкость — умеренно (не превращаем игру в кислоту)\n\n"
        "3) Мувмент + стрельба:\n"
        "• Стрейф-контроль во время трекинга\n"
        "• Пик: 1 инфа → 1 килл → смена позиции\n"
    )


def wz_movement_positioning_text(profile: Dict[str, Any]) -> str:
    diff = _p(profile, "difficulty", "Normal")
    return (
        "🧠 Warzone — Мувмент/Позиционка (RU)\n\n"
        f"Режим: {diff}\n\n"
        "СЕЙЧАС (в бою):\n"
        "• Не стой в простреле дольше 2–3 сек\n"
        "• Репик = смерть. Меняй угол после урона/кила.\n"
        "• Стреляй → сместись → снова стреляй.\n\n"
        "ДАЛЬШЕ (привычка):\n"
        "• Каждые 15 сек задавай себе вопрос: «Где мой выход?»\n"
        "• Если выхода нет — ты уже труп, просто пока живёшь.\n"
    )


def wz_audio_visual_text(profile: Dict[str, Any]) -> str:
    return (
        "🎧 Warzone — Аудио/Видео (RU)\n\n"
        "Аудио:\n"
        "• Сделай шаги заметными (но не убей весь остальной звук)\n"
        "• Не меняй пресеты каждый день\n\n"
        "Видео:\n"
        "• Приоритет — читаемость врага, а не красота\n"
        "• Меньше визуального мусора = больше киллов\n\n"
        "Если хочешь — напиши «платформа + монитор/тв» и я дам конкретику.\n"
    )
